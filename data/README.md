# Vendor data

Canonical contribution files live at `vendors/{shard}/{slug}.yaml`.

The shard is the first two characters of the vendor slug. Use an existing file
as the authoring example. The canonical executable schema belongs to Sourcey's
Catalog engine and is enforced by the digest-pinned verifier in pull-request
CI; it is intentionally not copied into this repository.
